javascript only functional scope and global scope. There is no block level scope.
let and const declare block level scope

let c;     
const b = 10;   this will be initialize the value (const b; Gives the error)

let c = 10
let c = 20;  gives the error